/**
 * Config overlay — initial setup panels.
 * Config functions are defined in app.js for direct template access.
 * This module is a placeholder for future config-specific logic.
 */
export function setupConfigOverlay(store) {
  return {};
}
